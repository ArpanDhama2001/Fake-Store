import CartContainer from "../components/CartContainer";

const Cartpage = () => {
  return (
    <main className="mx-auto">
      <div>
        <CartContainer />
      </div>
    </main>
  );
};

export default Cartpage;
